version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - PATH=./node_modules/.bin:$PATH npm run build
cache:
  paths:
    - 'node_modules/**/*'
artifacts:
  files:
    - build/**/*
  name: spotifaves-ui-$(date +%Y-%m-%d)
